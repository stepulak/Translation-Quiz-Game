package com.stepulak.translationgame;

// Used as a key for further UIElement access in UIManager
// Order does matter!
public enum UIElementType {
    BACKGROUND,
    KEYBOARD,
    INPUT_FORM,
    WORD_LABEL,
    VERSION_LABEL,
    VERTICAL_MENU,
    QUIT_BUTTON,
    SKIP_BUTTON,
    CLEAR_BUTTON,
    TIMER,
    GAME_MENU_TITLE,
    LANGUAGE_SET_LABEL,
    DELIMITER
}
